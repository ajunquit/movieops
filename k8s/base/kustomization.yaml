apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Every resource from every referenced directory gets this namespace stamped
# on automatically — no need to repeat `namespace: movieops` in each file.
namespace: movieops

resources:
  - namespace.yaml
  - backend
  - frontend
  - ingress
